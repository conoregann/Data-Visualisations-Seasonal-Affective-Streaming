# Data

Place the raw Spotify dataset here as:

```text
spotify_data.csv
```

The CSV is excluded from Git because it is too large for a normal GitHub repository. The report expects this path when reproducing the analysis locally.

